= 符号约定

为了便于表述，本文相关的符号约定如表@符号约定 所示。

#figure(
  table(
    columns: 2,
    stroke: (x: none, y: none),
    align: (center, center, center),
    inset: 10pt,
    table.hline(),
    table.header([*Symbol*], [*Description*]),
    table.hline(),
    [$C_j P_i$], [编号为$j$的CCU所控制的编号为$i$的输入端口],
    [$P_j P_i$], [编号为$j$的PCU所控制的编号为$i$的输出端口],
    table.hline(),
  ),
  caption: [符号约定]
) <符号约定>
